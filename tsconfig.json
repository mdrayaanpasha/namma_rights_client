{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  
    "compilerOptions": {
      "jsx": "react-jsx", // or "react" if using an older setup
      "skipLibCheck": true // (optional) Avoids type checking in dependencies
    }
  
  
}
